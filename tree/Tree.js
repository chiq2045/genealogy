const Person = require('./Person');

/**
 * Class representing a tree
 */
class Tree {
  /**
   * @class
   * @property {Person} root - the root of a tree
   */
  constructor() {
    this.root = null;
  }
}

module.exports = Tree;
